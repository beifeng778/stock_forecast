# sample-gen 本地覆盖配置示例（建议只放“你本机需要覆盖”的少量配置）
# 复制为 sample-gen/.env.local 后生效。
# 注意：sample-gen 会先加载 .env，再加载 .env.local（.env.local 覆盖优先）。

# 本地开发：不需要守护进程，直接跑脚本生成一次即可
LLM_SAMPLE_GEN_DAEMON=false

# 输出路径（本地开发通常写：../rag，最终写入 ../rag/llm_samples.db）
LLM_SAMPLES_PATH=../rag

# 本地快速验证：只生成少量股票/较短历史（需要全量时直接删掉这些覆盖项）
LLM_SAMPLE_GEN_MAX_STOCKS=20
LLM_SAMPLE_GEN_DAYS=60
