# sample-gen（蒸馏样本生成）环境变量示例
# 复制为 .env 或 .env.local 后使用。
# 注意：sample-gen 会先加载 .env，再加载 .env.local（.env.local 优先生效）。

# 是否启用 sample-gen（默认 true）
LLM_SAMPLE_GEN_ENABLED=true

# 是否以守护进程模式运行（默认 false）
LLM_SAMPLE_GEN_DAEMON=true

# 每天定时执行时间（HH:MM，24小时制，默认 04:20）
LLM_SAMPLE_GEN_TIME=04:20

# 启动时是否立即执行一次（默认 false）
LLM_SAMPLE_GEN_ON_STARTUP=true

# 失败重试次数与重试间隔（分钟）
LLM_SAMPLE_GEN_RETRY_COUNT=3
LLM_SAMPLE_GEN_RETRY_INTERVAL=10

# 样本库输出路径：可写目录或 db 文件路径
# - Docker/部署环境推荐：/app/rag（最终写入 /app/rag/llm_samples.db）
# - 本地开发可用：../rag
LLM_SAMPLES_PATH=/app/rag

# 样本生成规模控制
# - LLM_SAMPLE_GEN_MAX_STOCKS=0 表示全量股票；>0 表示取前 N 只
# - LLM_SAMPLE_GEN_DAYS 表示每只股票回溯天数（默认 180）
# - LLM_SAMPLE_GEN_MIN_HISTORY 表示计算指标最少历史长度（默认 30）
# - 默认行为：增量写入现有 llm_samples.db（只补新交易日样本）
# - 若需要强制全量重建：LLM_SAMPLE_GEN_REBUILD=true（会重建临时库并原子替换）
LLM_SAMPLE_GEN_MAX_STOCKS=0
LLM_SAMPLE_GEN_DAYS=180
LLM_SAMPLE_GEN_MIN_HISTORY=60
LLM_SAMPLE_GEN_REBUILD=false
